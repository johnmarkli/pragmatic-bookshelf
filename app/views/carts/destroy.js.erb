// Hide cart if emptied and length is 1
if ($('#cart tr').length > 0) { $('#cart').hide("blind", {direction: "vertical" }, 1000); }

// leave total line so that items can be added again based on js logic of showing cart when there is one new line in create.js.erb
$('#cart tr').not('.total_line').remove();